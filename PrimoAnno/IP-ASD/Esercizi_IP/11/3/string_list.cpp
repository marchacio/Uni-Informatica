#include "string_list.h"

int main() {

  list l = new cell;

  headInsert(l, "gino");
  headInsert(l, "pino");
  headInsert(l, "gufo");

  //read(l);

  //getElem(l, 0);

  //insertAt(l, "pasquale", 20);

  print(l);


  return 0;
}
